<script>
	// import { onMount } from 'svelte';
  // import Theme from '@oneezy/ui/src/components/Theme/Theme.svelte';
	export let Hst

	// let loaded = false;
  // let isMatchMedia = typeof window.matchMedia === 'function';
	
	// onMount(() => {
	// 	loaded = true;
	// });
</script>

<Hst.Story group="components" title="Molecules/Theme" layout={{ type: 'single', iframe: false }}>
  
  <!-- {#if loaded && isMatchMedia}
    <Theme />
  {/if} -->
  
</Hst.Story>